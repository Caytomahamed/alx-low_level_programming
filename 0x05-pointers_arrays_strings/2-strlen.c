#include "main.h"
/**
 * _strlen - Get length of string
 * @s: is string that count is length
 *
 * Return: length of string
*/
int _strlen(char *s)
{
	int i;
	int len;

	for (i = 0; s[i] != '\0'; i++)
	{
		len = i;
	}

	return (len + 1);
}
